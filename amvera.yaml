meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
  projectType: web

run:
  persistenceMount: /data
  exposedPort: 8000
  command: uvicorn main:app --host 0.0.0.0 --port 8000 --app-dir /app/server
