meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: server/requirements.txt
run:
  persistenceMount: /data
  command: uvicorn main:app --host 0.0.0.0 --port 80 --app-dir server
  containerPort: 80
